name: lemon_engine_tutorial
description: An example game using the lemon engine

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  lemon_engine: 0.0.2

  flutter:
    sdk: flutter

flutter:
  uses-material-design: true

